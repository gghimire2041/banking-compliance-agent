# Core web framework
streamlit>=1.28.0

# LLM and AI frameworks
llama-index>=0.9.0
llama-index-llms-openai>=0.1.0
llama-index-embeddings-openai>=0.1.0
openai>=1.0.0

# Data processing
pandas>=1.5.0

# Configuration
python-dotenv>=1.0.0

# New dependencies for ReAct system
# Database (SQLite is built into Python, no additional package needed)
# JSON handling (built into Python)
# Datetime (built into Python)
# Regex (built into Python)
# Typing (built into Python)
# Dataclasses (built into Python, Python 3.7+)
# Enum (built into Python)

# Optional: Enhanced features
requests>=2.28.0  # For future API integrations
pydantic>=2.0.0   # For better data validation